name: PR Summarizer
description: Fetches a PR diff and posts an AI-generated summary as a comment using Claude

inputs:
  github-token:
    description: GitHub token for API access and posting comments
    required: true
  anthropic-api-key:
    description: Anthropic API key for Claude access
    required: true

runs:
  using: node20
  main: index.js
